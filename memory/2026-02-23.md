

## 추가 적용 4 (2026-02-23T17:49:16+09:00)
- Playwright 부하 완화: daily_korean_news.py에 환경변수 기반 제어 추가
  - CONCURRENCY (default=1): 향후 사용을 위해 읽도록 구현(현재락+nice로 단일 인스턴스 보장)
  - MAX_PER_RUN (default=3): 한 번 실행에서 Playwright 폴백을 사용할 최대 기사 수로 제한
  - PAGE_DELAY (default=3.0s): 페이지 방문/Playwright 호출 사이에 대기시간을 삽입하여 CPU 스파이크 완화
- 구현 세부사항:
  - items를 처리할 때 MAX_PER_RUN 단위로 배치 처리하고, 각 배치 후 PAGE_DELAY만큼 sleep을 추가함
  - Playwright 호출 직전에도 PAGE_DELAY를 존중하도록 수정하여 연속적 브라우저 실행 빈도를 낮춤
- 기대효과: Playwright(Chromium)로 인한 순간적인 CPU 스파이크를 줄여 시스템 안정성을 높임

---

## 추가 적용 5 (2026-02-23T22:16:00+09:00)
- Apple Calendar 우선화 및 데일리 브리핑 안정화
  - run_daily_schedule.sh 수정: .env 명시적 source, icalBuddy 우선 사용, 실패 시 osascript(파이썬 래퍼)를 폴백하도록 구현
  - calendar_read.py 추가: osascript를 안전히 호출해 날짜 범위로 이벤트를 추출하는 보조 스크립트 생성
  - 메시지 포맷 일관화(일정 없을 때의 메시지 포함)
- Apple Calendar 검증 결과: 캘린더 목록 및 다음 7일 일정 정상 조회(icalBuddy로 확인). 캘린더 권한은 허용된 상태로 판별

- Telegram 연동·테스트 및 안정화
  - .env 관리: /Users/jamie/.openclaw/workspace/daily-korean-news/.env 에 TELEGRAM_BOT_TOKEN/TELEGRAM_CHAT_ID 보관; 여러 번 테스트하며 토큰 빈값/갱신 이슈 발견 및 수정
  - getMe/getUpdates/sendMessage로 토큰 검증 진행, 초기 404 원인은 토큰이 비어있던 세션에서 호출되어 발생함(적절히 .env를 source하면 해결됨)
  - 데일리 브리핑(오늘/내일/다음7일) 텍스트 전송을 수동으로 검사하고 실제 Telegram 전송(여러 메시지 ID 확인) 성공

- 이미지 표 렌더링 파이프라인 추가
  - Pillow 설치 시도 및 환경(venv) 검사: /Users/jamie/.venvs/daily-news 에 Pillow 설치 시도, 일부 빌드 이슈 발생하여 대체 판본 사용 또는 이미 설치된 Pillow 경로로 실행
  - 한글 폰트 확인 및 적용: AppleSDGothicNeo, AppleGothic, NotoSansGothic 사용 가능 확인
  - render_table_image.py 작성: icalBuddy 또는 calendar_read.py로 다음 7일 일정을 가져와 스타일된 PNG(/tmp/calendar_table_full.png)를 생성
  - 캡션 인코딩 문제 해결: 전송 시 export LANG=ko_KR.UTF-8 LC_ALL=ko_KR.UTF-8 및 curl --form-string 사용으로 한글 깨짐 방지
  - 생성된 이미지(/tmp/calendar_table_ko.png, /tmp/calendar_table.png, /tmp/calendar_table_full.png)를 수동/자동으로 전송 테스트하여 정상 표시 확인 (메시지 IDs 기록됨, 마지막 이미지 전송 성공 message_id=808)

- 인터랙티브 버튼 및 콜백 처리
  - 메시지에 인라인 키보드 추가(예: 전체 보기, 알림 설정)
  - callback_poller.py 작성 및 확장(폴링 방식)
    - getUpdates 폴링, offset 관리(/tmp/telegram_poll_offset.txt)
    - view_all: image(있으면) 전송 또는 텍스트 요약 전송
    - notify_settings: 간단 토글/메뉴 응답 및 설정 파일 저장 스켈레톤(/Users/jamie/.openclaw/workspace/daily-schedule/settings.json)
  - LaunchAgent 등록: ~/Library/LaunchAgents/com.jamie.telegram-callback-poller.plist 로 poller 자동 시작(KeepAlive, Aqua 세션)
  - poller의 동작을 여러 차례 테스트하고 로그(/var/log/telegram-callback-poller.log, .err.log)로 문제점 점검

- 프로세스/운영 조치
  - 중복/걸린 테스트 프로세스 정리(pkills 등) 및 venv 활성화해서 테스트 반복
  - launchd 에이전트 재로드 및 수동 실행을 통한 엔드-투-엔드 검증

- 남은 작업 및 권장 사항
  - notify_settings 실제 저장/반영 로직 완성(현재 스켈레톤) — 사용자의 선택을 settings.json에 기록하고 poller가 이를 참고해 알림 동작을 제어하도록 구현 권장
  - 이미지 템플릿 추가(로고, 색상 테마, 모바일 최적화) — 현재 기본 템플릿 적용됨; 추가 디자인 옵션 제공 가능
  - 웹훅 전환 옵션: 실시간성 및 확장성 향상을 위해 외부 HTTPS 엔드포인트(webhook)로 전환 검토 가능(요청 시 안내/설정)
  - 관찰 기간 권장: 변경 후 24시간 관찰하여 시스템 모니터링(Playwright 부하, launchd 작업, Telegram 전송 성공률)을 권장

- 생성/수정된 주요 파일
  - modified: daily-schedule/run_daily_schedule.sh (Apple Calendar 우선화, .env 명시적 로드)
  - created: daily-schedule/calendar_read.py (AppleScript 래퍼)
  - created: daily-schedule/render_table_image.py (PNG 렌더러, Pillow 사용)
  - created: daily-schedule/callback_poller.py (폴링 기반 콜백 처리기)
  - created: ~/Library/LaunchAgents/com.jamie.telegram-callback-poller.plist
  - updated: daily-schedule/run_daily_schedule.sh (통합 및 로케일 안전 처리)

---

## 추가 적용 6 (2026-02-23T22:24:00+09:00)
- callback_poller.py 확장 및 LaunchAgent 등록
  - callback_poller.py를 이미지 전송(view_all → /tmp/calendar_table_full.png 우선)과 notify_settings 응답으로 확장
  - settings.json(채팅별 알림 모드) 스켈레톤 추가 및 저장 로직 추가
  - ~/Library/LaunchAgents/com.jamie.telegram-callback-poller.plist 생성 및 로드(KeepAlive, Aqua)

## 추가 적용 7 (2026-02-23T22:33:00+09:00)
- 이미지 렌더러 및 전송 테스트
  - render_table_image.py로 /tmp/calendar_table_full.png 생성(AppleSDGothicNeo 사용)
  - 로컬에서 이미지 생성 확인 및 MD5 해시 기록
  - sendPhoto 테스트: 일부 세션에서 .env 로드 이슈로 404 발생; 사용자의 터미널에서 재전송으로 정상 전송 확인(message_id=808)

## 추가 적용 8 (2026-02-23T22:49:00+09:00)
- alert_dispatcher 추가(15분 전 알림)
  - daily-schedule/alert_dispatcher.py 작성: 5분 간격(LaunchAgent StartInterval=300)으로 실행하여 다음 16분 내 시작 이벤트를 조회해 알림 발송
  - ~/Library/LaunchAgents/com.jamie.daily-alert-dispatcher.plist 생성 및 로드
  - 수동 테스트 결과: 해당 시간대에 일정이 없어 알림 없음(출력: "No upcoming events in next 16 minutes")

## 추가 적용 9 (2026-02-23T22:56:00+09:00)
- 메시지 포맷 개선(코드블록 → 고정폭 표 + 인라인 버튼)
  - run_daily_schedule.sh을 수정하여 기본적으로 고정폭 코드블록 포맷을 사용하고 인라인 버튼(전체 보기, 알림 설정)을 포함하도록 변경
  - 전송/파싱 테스트를 반복하여 Markdown 인코딩(특수문자 이스케이프)과 로케일 문제 해결

## 추가 적용 10 (2026-02-23T23:00:00+09:00)
- 사용자 지정 출력 규칙 반영 및 파서 보강
  - 사용자의 출력 규칙(날짜 그룹화, 한 줄 요약 형식, 라벨/이메일 제거, 24시간제, 중복 제거, 공휴일 아이콘 등)을 반영한 파서 로직을 run_daily_schedule.sh의 Python 헬퍼에 구현
  - calendar_read.py의 원시 출력을 저장(/tmp/ical_raw.txt)하고 이를 기준으로 파싱 테스트 및 보강을 반복
  - 여러 차례 테스트 전송(메시지 IDs: 816, 821, 822, 823, 824, 825, 826, 827, 828, 829)을 통해 포맷 안정화

## 추가 적용 11 (2026-02-23T23:12:00+09:00)
- 삼일절 아이콘 변경
  - 공휴일(특히 삼일절) 탐지 로직을 추가해 기본 공휴일 아이콘을 🇰🇷(대한민국 국기)로 표시하도록 변경
  - run_daily_schedule.sh에 반영 및 테스트 전송(message_id=829)

---

(끝)
