# 2026-03-14

- Built and iteratively refined Jamie's personal dashboard project at `~/Projects/my-dashboard`.
- Confirmed OpenClaw core/latest version as `2026.3.12`.
- Dashboard stack/state: Next.js 15 + TypeScript + Tailwind; lint/build passing; launchd service configured and running on port 3000.
- Implemented live OpenClaw command bridge via local `openclaw agent --local` CLI with later stabilization using ephemeral session IDs and shorter timeout; fallback remains in place when bridge is slow/unavailable.
- Connected Apple Calendar into dashboard via local Calendar access; dashboard shows today’s schedule and can deep-link into Calendar.
- Connected Obsidian default vault/workflow: current default vault aligned to `/Users/jamie/Library/Mobile Documents/iCloud~md~obsidian/Documents/work`; dashboard supports recent notes and title search; note items open in Obsidian.
- Registered dashboard as persistent macOS LaunchAgent service: `~/Library/LaunchAgents/com.jamie.my-dashboard.plist` with start script in `~/Projects/my-dashboard/scripts/start-dashboard.sh`.
- Added dashboard PWA/app-like features: manifest, icon, service worker, offline page, mobile action bar, and app-style header polish.
- Upgraded dashboard settings from browser-only state to server/file-backed storage via `data/dashboard-settings.json` and `/api/settings`.
- Expanded stock watchlist from fixed list to searchable near-full market universe using `/api/stocks/search`, backed by cached KOSPI/KOSDAQ listing data; watchlist add/remove now persists through server settings.
- Reworked dashboard layout multiple times per Jamie’s preference: weather moved into header, todo moved into header, then unified header modules to include message/command, time, weather, and todo within the main dashboard surface.
- Latest layout-related commits today included: `269148a`, `9d4a0a2`, `ff6f540`, `18f2488`, `076bd83`, `abf289e`, `c068465`, `6d4ba31`, `eb5d621`, `0b58ec9`, `e61f991`, `b26e04d`, `aca0aa1`.
- Jamie strongly prefers iterative visual/layout polish with practical utility over placeholder status text; specifically asked to remove the old "9단계 / 명령 브리지…" header status copy and replace dead space with useful modules.
- 15:11 Apple Calendar `family`에 오늘 15:30~17:30 일정 "가족나들이" 추가.

- 2026-03-14 15:33:49 KST 캘린더 Family에 일정 추가: 2026-03-15 17:00-19:00, 영화보기, 장소 세종CGV
