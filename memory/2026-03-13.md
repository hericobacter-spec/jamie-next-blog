---
created: 2026-03-13
timezone: Asia/Seoul
---

## 작업 로그
- 06:39경 사용자 확인 후, 아침 통합 브리핑 요구사항을 재확인함: 매일 06:00에 날씨 + 미확인 이메일 + 오늘 일정 + 주요 뉴스를 한 메시지로 자동 발송.
- 신규 스크립트 작성: `daily-schedule/morning_briefing.py`
  - 날씨: Open-Meteo 기반 현재 기온/체감/강수확률
  - 이메일: `gog gmail search` 기반 미확인 메일 3건 요약
  - 일정: Apple Calendar(icalBuddy 우선) 기반 오늘 일정 추출
  - 뉴스: Google News RSS 상위 10건 제목 요약
- 신규 LaunchAgent 작성: `~/Library/LaunchAgents/com.jamie.morning-briefing.plist`
  - 스케줄: 매일 06:00
- 기존 분리 발송 작업 중복 방지를 위해 기존 07시 일정/날씨 LaunchAgent를 bootout 처리함.
- 테스트 출력 형식을 조정하여 이메일 개수/발신자 표시 및 일정 시간 24시간제 표기로 보정함.
- 통합 브리핑 테스트 발송 성공(Telegram message_id=2997).
- 장애 원인 분석 정리: 2026-03-12 심야~2026-03-13 새벽 무응답의 주원인은 `openai-codex` OAuth 토큰 갱신 실패(만료/refresh_token_reused)였고, 여기에 Gateway의 SIGTERM 재시작이 겹치며 메인 Telegram 세션 응답 누락/지연이 발생한 것으로 판단함.
- 재발 방지 포인트: 갑작스러운 무응답이 생기면 기능 코드보다 먼저 `~/.openclaw/logs/gateway.err.log`의 인증 오류와 `~/.openclaw/logs/gateway.log`의 SIGTERM/restart 흔적을 우선 확인.
- 로또 자동구매 판정 로직 보강: `scripts/lotto_auto_buy.js`가 실행 결과를 `[LOTTO_RESULT] {status: ...}` 형태로 명시 출력하도록 수정함. 이제 `buy_success`, `weekly_limit_reached`, `buy_failed`, `result_unknown`, `login_required`, `script_error`, `dry_run_ready`를 구분 가능.
- 로또 자동구매 보고 래퍼 추가: `scripts/lotto_auto_buy_report.py` 작성. 목요일 16:00 예약 작업이 이제 이 래퍼를 실행해 사람이 읽기 쉬운 결과 문구만 텔레그램으로 보고하도록 변경함.
- Codex CLI 설치 및 OAuth 로그인 완료: `npm install -g @openai/codex` 후 `codex login --device-auth`로 ChatGPT 계정 연동을 완료함. 이후 Codex로 실제 코딩 작업 수행 가능 상태가 됨.
- 신규 프로젝트 생성: `~/Projects/my-dashboard` 에 개인 대시보드 웹앱 생성.
  - Next.js 15 + TypeScript + Tailwind 기반
  - 기본 다크 테마, 반응형 레이아웃, 헤더/카드 구조 구현
  - lint/build 통과 상태까지 정리
- 대시보드 구현 진행 상황:
  - Step 1~2: 기본 UI + 시계 + BTC 위젯 + `/api/btc`
  - 이후 BTC를 국내 증시 위젯 방향으로 전환
  - 한/영 전환 기능 추가(기본 한국어), `src/lib/dashboard-copy.ts` 기반 카피 구조화
  - 할일 위젯, 날씨 위젯, 뉴스 위젯, 메시지/명령 입력 위젯(mock 구조) 추가
  - 시계를 별도 카드에서 헤더(`내 대시보드`) 내부로 이동, 메시지 명령창은 본문 상단 유지
- 대시보드 개발 서버 운영 메모:
  - `npm run dev` 중복 실행으로 3000/3001 포트 혼선이 여러 차례 발생함.
  - 동일 프로젝트의 `next dev` 프로세스를 정리한 뒤 3000 포트 하나만 사용하도록 재기동함.
- 공공데이터 금융 API 문서 검토:
  - `GetMarketIndexInfoService/getStockMarketIndex` 문서로 KOSPI/KOSDAQ 등 지수 데이터 조회 가능함을 확인
  - `GetStockSecuritiesInfoService/getStockPriceInfo` 문서로 개별 종목(단축코드 기반) 조회 가능함을 확인
  - 현재 대시보드의 국내 증시/관심종목 위젯을 공공데이터 API 기반으로 교체하는 작업을 시작함
  - 관심종목 편집 파일은 `src/lib/korean-stock-watchlist.ts`
