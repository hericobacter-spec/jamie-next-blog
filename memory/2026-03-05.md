---
created: 2026-03-05
timezone: Asia/Seoul
---

## SmartThings ↔ Home Assistant / switch.geosil1
- User reports SmartThings app controls device normally; HA entity `switch.geosil1` observed as `unavailable` via HA API.
- Reminded user to check HA Settings → Devices & services → SmartThings (Reload / re-auth) and entity integration details after work.
- One-shot reminder scheduled for 20:00 KST to prompt HA SmartThings integration check.

## Gas price lookup (Sejong)
- Looked up Sejong city gasoline prices via Opinet; also checked Saerom-dong SK “새롬주유소” price via Oilnow.

## Lotto auto-buy automation (dhlottery)
- Cron job created: every Thu 16:00 KST, attempt Lotto 6/45 auto-buy 5 games (₩5,000), reporting outcome to Telegram.
- Implemented macOS Keychain-based auto-login in `scripts/lotto_auto_buy.js`:
  - Reads generic password from Keychain service `openclaw-dhlottery`, account `auqas`.
  - Updated login URL to `https://www.dhlottery.co.kr/login` and selectors `#inpUserId`, `#inpUserPswdEncn`.
- Keychain access required “Always Allow”; user enabled it.
- Fixed headless UI issue: auto-select checkbox could be outside viewport; toggled via DOM `evaluate` with click/change events.
- Added `--keep-open` option to keep browser open up to 10 minutes for manual inspection when result unclear.
- Debugging: captured screenshots on ambiguous outcomes under `memory/lotto-unknown-*.png`.
- Discovered purchase confirmation popup text “구매하시겠습니까” requires clicking “확인”; added broader confirm-click heuristics (dialog/layer/frame).
- Screenshot showed post-purchase receipt popup titled “구매내역 확인” with issue date/time and A–E numbers (suggests successful purchase). Subsequent buy attempts likely failed due to weekly limit/policy; success detection logic needs to treat receipt popup as success.
