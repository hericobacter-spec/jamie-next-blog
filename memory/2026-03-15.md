2026-03-15 — 대시보드 작업 로그

- 오전: 대시보드(~/Projects/my-dashboard) 개선 작업 수행
  - 기능 추가/수정:
    - 다크모드 추가: 새 테마 `classic-dark`을 도입하고 Tailwind/CSS 변수(css file: src/app/globals-dark.css)로 색상·대비 설정을 정의함. 기존 테마(aurora-glass, apple-light)는 변경하지 않음.
    - 할일(Todo) 위젯: draft 상태 도입, Save(저장)/Revert(되돌리기) 버튼 추가 및 서버 저장(sync to data/dashboard-settings.json via /api/settings). 버튼은 변경사항이 있을 때 활성화되도록 구현.
    - 모바일 개선: 모바일 하단 액션바에 todo-save / todo-revert 이벤트 디스패치 추가, Todo 위젯에서 이벤트 리스닝해 모바일에서도 저장/되돌리기 동작 가능하게 함.
    - SSR 테마 동기화: 서버의 data/dashboard-settings.json을 읽어 SSR 시점에 <html data-theme=...>에 반영하도록 src/app/layout.tsx 수정.
    - UI/스타일: Save/Revert 버튼 가시성·포커스·호버 스타일 개선, classic-dark 색상·대비 미세조정.
  - 커밋: 여러 커밋으로 변경사항 저장(예: feat: add classic dark theme; feat(todo): add save/revert; style updates; chore(settings) 등).

- 개발서버 운영/디버깅:
  - dev 서버를 여러 번 재시작했고 포트 충돌(3000) 문제를 조사하여 포트 점유 프로세스(node PID 57165)를 종료하고 dev를 PORT=3000으로 재시작하여 정상 가동시킴.
  - /api/settings과 /api/market 엔드포인트 동작 확인(POST/GET, 시장 데이터 호출 확인). 일부 500 에러(개발 중 .next 캐시/번들 문제)는 .next 캐시 정리 및 dev 재시작으로 해결됨.

- 테스트·검증:
  - 데스크탑: 다크모드, Todo 저장/되돌리기 동작 확인됨. Save 누르면 /api/settings에 POST되어 data/dashboard-settings.json에 반영되고 새로고침 후에도 유지됨.
  - 모바일: 초기에는 테마가 2개만 보이는 문제, Save/Revert 버튼 노출 문제, 주식 위젯 로딩 문제 등 보고됨. 모바일 전용 토글 로직 보강, 모바일 액션바에 save/revert 이벤트 추가, SSR에서 서버 테마 반영 등으로 개선 적용. SSR HTML now reflects theme from data/dashboard-settings.json.

- 권고 및 다음 단계:
  - 모바일에서 실제 동작(토글/저장/주식 위젯 렌더)을 최종 확인할 필요 있음. (자동화 시나리오 수행 가능)
  - 동시사용/충돌 방지: settings 파일 기반 단일 사용자 용도로는 충분하나 멀티클라이언트 동시 수정 고려 시 버전/timestamp 검증 추가 권장.

- 사용자 요청 요약(오늘):
  - 다크모드 추가(새 테마, 기존 테마 보존)
  - Todo: Save 버튼 및 Revert 버튼 추가, 새로고침 시 리셋 문제 해결
  - 모바일 적용 및 주식 위젯 문제 점검
  - dev 서버 포트 3000으로 복구

- 메모 생성: 2026-03-15 03:43 UTC / 12:43 KST

---
2026-03-15 16:10 KST — Pre-compaction flush
- Captured durable memories from today's session and developer work:
  - Added `classic-dark` theme and updated src/app/globals-dark.css; preserved existing themes.
  - Implemented Todo widget draft state with Save/Revert UI and server-backed persistence to data/dashboard-settings.json via /api/settings.
  - Mobile improvements: added mobile-action-bar events for save/revert and fixed mobile theme visibility; addressed stock widget loading on mobile.
  - SSR now reads data/dashboard-settings.json and sets <html data-theme=...> at render time.
  - Dev server restored to PORT=3000 after killing conflicting process; resolved .next cache bundling errors by clearing cache and restarting.
- Recommendation recorded: add version/timestamp validation to settings to handle concurrent edits.
- Source: session logs and today's work notes appended to this file.

---
2026-03-15 16:20 KST — Dashboard recovery & UI tweak
- Action summary:
  - Stopped automatic launchd retries for my-dashboard, identified and cleared port-3000 conflict, rebuilt the Next.js app, and reloaded the LaunchAgent to restore the service on localhost:3000.
  - Fixed EADDRINUSE symptom by unloading the LaunchAgent, ensuring no stale listeners remained, and confirming HTTP 200 from localhost:3000 after restart.
  - Adjusted dark-mode control colors: updated src/app/globals-dark.css to replace bright white control active background with an accent-tinted background (--control-active-bg -> rgba(102,168,255,0.12)) and a matching hover tone; rebuilt and verified successful production build.
- Outcome:
  - Dashboard service on port 3000 restored and responding. Production build compiled successfully.
  - UX improvement: dark-mode control toggle now uses a subtle accent tint instead of white, improving contrast and legibility in dark mode.
- Next steps / notes:
  - If you want a different tint or stronger contrast, I can iterate on the color values and redeploy.
  - Recommend adding a pre-start build check in the LaunchAgent script to avoid starting before .next exists.
- Source: interactive session with user and local logs.

---
2026-03-15 21:20 KST — Blog redesign & deployment work
- Started integrating timlrx/tailwind-nextjs-starter-blog into Jamie-blog repository to modernize layout and enable MDX content. Created working branches and performed staged merges.
- Created branches: feat/theme-aurora, feat/theme-aurora-clean, feat/starter-integration, feat/starter-clean, feat/yiyb-style for iterative development and cleanup.
- Added Aurora-like font stack and color tokens (tailwind.config.js) and global tweaks (styles/globals.css). Implemented Pretendard webfont support and a custom styles/fonts.css.
- Integrated starter layout/components and adapted yiyb-inspired post styles (post-content width, code block styles). Imported Pretendard .woff2 files into public/fonts/pretendard/.
- Resolved GitHub push failure due to large file in history by creating a clean snapshot and pushing feat/starter-clean, then merged that branch into main to prepare a clean deployable state.
- Addressed app vs pages router conflicts by backing up pages/ to backup/pages-removed/ and removing pages/ where appropriate, then restoring a clean app/pages structure suitable for Next.js routing.
- Performed local npm ci and next build checks; iteratively fixed TypeScript import issues by replacing '@/assets/fonts' imports with local public font paths and simplifying styled-components theme references.
- Merged styles selectively: copied Pretendard fonts and key style utilities from yiyb repo into a feature branch (feat/yiyb-style) and adjusted styles to be YIYB-inspired but customized (not exact copy).
- Executed full production builds in /tmp/jamie-blog-clean to verify successful build and static export readiness. Confirmed that https://hericobacter-spec.github.io/jamieblog/ responds 200 and serves the site.
- Created backups of removed files and large-change snapshots under /Users/jamie/.openclaw/workspace/Jamie-blog/backup/ and /tmp/jamie-blog-clean/ for recovery.

Notes & next steps
- Recommend final cleanup: prune starter-import/yiyb_source (keep as archived copy) and cherry-pick only the necessary style assets into a single clean branch for final PR.
- Optionally include Pretendard webfonts in repo (already added) and finalize font-face usage in globals to ensure consistent rendering.
- If you want exact YIYB parity, additional component and JS logic would need bringing in (but that increases complexity); current approach keeps styles only and avoids pulling full app logic.

Source: interactive session actions, git history, local build logs.

---
2026-03-15 22:59 KST — Memory flush entry
- Recorded the above development session: blog starter integration, style changes, font additions, git cleanup, build/test steps, and deployment confirmation to GitHub Pages.
- Backup paths: /Users/jamie/.openclaw/workspace/Jamie-blog/backup/ and /tmp/jamie-blog-clean/
- If additional durable details needed, I will append further notes.
