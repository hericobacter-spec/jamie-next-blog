2026-03-16 — 활동 요약

- 생성/변경한 주요 항목:
  - feat/yiyb-full 브랜치에서 Jamie-blog 리포지토리 통합 작업 진행.
  - .github/workflows/pages-deploy.yml 추가: pnpm 환경(actions/setup-node, pnpm/action-setup)으로 GitHub Pages 빌드 및 배포 설정 생성하고 커밋·푸시함.
  - sitemap 페이지가 static export를 방해해 임시로 파일명 변경/백업(_sitemap.xml.ts) 처리 및 generateFullURL 유틸 스텁 추가로 빌드 오류 수정함.
  - pnpm install, next build, next export로 정적 산출물(out/)을 생성하고 out/에 .nojekyll 추가 후 gh-pages 브랜치로 강제(push -f)하여 배포함.
  - next.config.js에 basePath:'/jamieblog' 및 assetPrefix:'/jamieblog' 설정을 임시 적용해 정적 자산 링크를 조정함.

- 상태 및 이슈:
  - 원격 gh-pages 브랜치에는 최신 정적 파일이 올라가 있으나 외부에서 접근 시(https://hericobacter-spec.github.io/jamieblog/) 404 응답이 발생함. 원인으로는 GitHub Pages 전파 지연, Pages 설정(branch/folder) 불일치, 또는 GitHub 내부 배포 실패 가능성이 있음.
  - 임시로 typescript.ignoreBuildErrors를 설정해 빌드 중단을 막았음(장기적으로 타입 오류 정리 권장).

- 다음 권장 조치:
  - GitHub Pages 설정 확인(Branch=gh-pages, Folder=root) 및 필요시 수정
  - Actions/Pages 배포 로그 확인 및 문제 있으면 재배포
  - 타입 오류 정리하여 ignoreBuildErrors 제거

기록자: Jarvis (자비스)
